{
  "name": "t206-signup-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker: email+password BigCommerce signup (no address) + SSO auto-login",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
